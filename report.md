# HW1 实验报告

> 课程作业主题：基于 NumPy 手写三层 MLP 的 EuroSAT 遥感图像分类

---

## 一、任务概述

本实验基于 EuroSAT RGB 数据集，使用 `NumPy` 从零实现一个三层多层感知机分类器，对 10 类遥感场景图像进行分类。整个实验过程中没有使用 `PyTorch`、`TensorFlow` 或 `JAX` 等框架提供的自动求导功能，而是手动实现了全连接层、激活函数、交叉熵损失、反向传播、SGD 优化器以及训练与测试流程。

EuroSAT RGB 数据集共包含 10 个类别、27000 张彩色遥感图像，每张图像大小为 `64 x 64 x 3`。本次作业的主要目标包括：

- 完成三层 MLP 的手写实现
- 完成训练、验证、测试与超参数搜索
- 输出测试集准确率与混淆矩阵
- 对第一层权重进行可视化
- 对测试集中的错分样本进行分析

---

## 二、数据处理

### 2.1 预处理流程

本实验的数据处理流程如下：

1. 读取 `EuroSAT_RGB` 目录下全部图像，并统一转换为 RGB 格式。
2. 将像素值从 `0~255` 归一化到 `[0, 1]`。
3. 将每张 `64 x 64 x 3` 图像拉平成长度为 `12288` 的一维向量，以适配 MLP 输入。
4. 使用分层抽样将数据划分为训练集、验证集和测试集，以保证各类别比例基本一致。
5. 使用训练集的均值和标准差对训练集、验证集和测试集进行标准化，避免数据泄漏并提升训练稳定性。

### 2.2 处理方式说明

上述操作属于图像分类任务中较为标准的预处理流程，不是针对本任务额外设计的创新方法。其主要作用是：

- 统一输入数值范围，提升优化稳定性
- 保证数据划分合理，避免类别分布失衡
- 避免训练集统计信息泄漏到测试阶段
- 将图像转化为 MLP 可以直接处理的向量形式

---

## 三、模型设计

### 3.1 网络结构

本实验实现的模型为严格意义上的三层 MLP，即包含 3 个带可学习参数的全连接层，结构如下：

```text
Input -> Linear(12288, h1) -> ReLU -> Linear(h1, h2) -> ReLU -> Linear(h2, 10) -> Softmax
```

其中：

- 输入维度：`12288`
- 输出维度：`10`
- `h1`：第一隐藏层维度
- `h2`：第二隐藏层维度

### 3.2 手写实现的基本组件

本实验中手动实现的核心组件包括：

- 全连接层 `Linear`
- 激活函数 `ReLU`、`Sigmoid`、`Tanh`
- `Softmax + CrossEntropy` 损失函数
- 分类准确率计算
- 小批量 `SGD` 优化器
- 学习率衰减策略
- `L2` 正则化
- 验证集最优权重保存机制

### 3.3 模型特点

由于本实验使用的是纯全连接结构，模型能够学习到一定的全局颜色与纹理统计特征，但对局部空间结构的建模能力明显弱于卷积网络。因此，本实验更适合用于验证从零实现神经网络训练流程，而不是追求遥感图像分类的最优性能。

---

## 四、实验设置

### 4.1 训练设置

| 项目 | 设置 |
| --- | --- |
| 优化器 | SGD |
| 损失函数 | Cross-Entropy Loss |
| 正则化 | L2 Weight Decay |
| 学习率衰减 | 每个 epoch 后乘以 `0.98` |
| Batch Size | `128` |
| Epoch 数 | `20` |
| 随机种子 | `42` |

### 4.2 超参数搜索空间

本实验采用网格搜索，共训练 27 组超参数组合：

- 学习率：`0.03`、`0.01`、`0.003`
- 隐藏层维度：`(256, 128)`、`(512, 256)`、`(1024, 512)`
- 权重衰减：`1e-3`、`1e-4`、`1e-5`
- 激活函数：`ReLU`

每组超参数对应一个独立 `trial`，并保存训练曲线、最优权重和验证结果。完成训练后，再对全部 27 个 trial 在同一测试集上统一评估，以分析超参数变化对模型性能的影响。

---

## 五、实验结果

### 5.1 最优模型结果

根据当前 27 组 trial 的统一测试结果，综合表现最好的模型为 `trial_07`。

| 指标 | 数值 |
| --- | --- |
| Trial | `trial_07` |
| 第一隐藏层维度 | `1024` |
| 第二隐藏层维度 | `512` |
| 激活函数 | `ReLU` |
| 学习率 | `0.03` |
| 权重衰减 | `0.001` |
| 验证集准确率 | `0.8687` |
| 测试集准确率 | `0.6923` |
| 测试集损失 | `0.9703` |

从结果可以看出，在本实验设置下，较大的隐藏层维度配合相对较大的初始学习率能够取得更好的分类性能，而过小的网络容量会明显限制模型表达能力。

---

### 5.2 超参数搜索结果分析

从 27 组试验的整体结果可以观察到以下规律：

1. 隐藏层规模对性能影响显著。`(1024, 512)` 这一组隐藏层设置整体表现最好，多组 trial 的测试准确率都接近 `0.69`。
2. 学习率过小时性能下降明显。`0.003` 对应的大部分组合测试精度低于 `0.66`，说明更新速度偏慢，难以充分逼近更优解。
3. 较大的权重衰减并未显著损害性能。在最优模型中，`weight_decay=1e-3` 反而取得了最好的测试结果。
4. 较小模型 ` (256, 128)` 的表现整体偏弱，说明纯 MLP 在处理遥感图像时仍然依赖足够的隐藏层宽度来提升拟合能力。

下图展示了全部 trial 的测试精度排序以及验证集准确率与测试集准确率之间的对应关系。

<div align="center">
  <img src="outputs/batch_test_summary/sorted_test_accuracy.png" alt="All Trial Test Accuracy">
  <p><em>图 1  不同 trial 的测试集准确率排序结果</em></p>
</div>

<div align="center">
  <img src="outputs/batch_test_summary/val_vs_test_scatter.png" alt="Validation vs Test Accuracy">
  <p><em>图 2  验证集准确率与测试集准确率的对应关系</em></p>
</div>

为了进一步观察不同隐藏层规模下学习率与权重衰减的影响，绘制了如下热力图。

<div align="center">
  <img src="outputs/batch_test_summary/heatmap_h256_128.png" alt="Heatmap h256 128">
  <p><em>图 3  隐藏层规模为 `(256, 128)` 时的测试精度热力图</em></p>
</div>

<div align="center">
  <img src="outputs/batch_test_summary/heatmap_h512_256.png" alt="Heatmap h512 256">
  <p><em>图 4  隐藏层规模为 `(512, 256)` 时的测试精度热力图</em></p>
</div>

<div align="center">
  <img src="outputs/batch_test_summary/heatmap_h1024_512.png" alt="Heatmap h1024 512">
  <p><em>图 5  隐藏层规模为 `(1024, 512)` 时的测试精度热力图</em></p>
</div>

---

### 5.3 训练过程分析

最优模型 `trial_07` 的训练损失曲线和准确率曲线如下所示。

<div align="center">
  <img src="outputs/trial_07/loss_curve.png" alt="Loss Curve">
  <p><em>图 6  最优模型的训练集与验证集损失曲线</em></p>
</div>

<div align="center">
  <img src="outputs/trial_07/accuracy_curve.png" alt="Accuracy Curve">
  <p><em>图 7  最优模型的训练集与验证集准确率曲线</em></p>
</div>

从训练过程可以观察到：

- 训练损失和验证损失整体呈下降趋势，说明模型能够逐步学习到具有判别性的表示。
- 验证准确率随着训练推进逐步上升，并在后期趋于稳定，说明模型已经基本收敛。
- 训练集与验证集之间存在一定差距，但没有出现非常剧烈的分离，因此当前训练设置下并未出现极端过拟合。

---

### 5.4 混淆矩阵分析

最优模型在测试集上的混淆矩阵如下所示。

<div align="center">
  <img src="outputs/trial_07/confusion_matrix.png" alt="Confusion Matrix">
  <p><em>图 8  最优模型在测试集上的混淆矩阵</em></p>
</div>

根据统计结果，较常见的混淆类别对包括：

- `PermanentCrop -> HerbaceousVegetation`，共 `77` 次
- `Highway -> River`，共 `53` 次
- `SeaLake -> Forest`，共 `52` 次
- `PermanentCrop -> AnnualCrop`，共 `50` 次
- `AnnualCrop -> HerbaceousVegetation`，共 `47` 次
- `River -> Highway`，共 `42` 次

这些混淆具有较强的遥感图像语义合理性：

- `Highway` 与 `River` 都可能在俯视图中呈现狭长、连续的带状结构。
- `PermanentCrop`、`AnnualCrop` 与 `HerbaceousVegetation` 均包含大量绿色纹理区域，颜色与纹理统计特征接近。
- `SeaLake` 被误分为 `Forest`，说明模型在某些样本中未能稳定地区分水体边界、阴影和周边地物背景。

---

### 5.5 权重可视化与空间模式观察

将最优模型第一层隐藏层的部分权重恢复为图像后，可得到如下可视化结果。

<div align="center">
  <img src="outputs/trial_07/first_layer_weights.png" alt="First Layer Weights">
  <p><em>图 9  第一层隐藏层权重恢复后的可视化结果</em></p>
</div>

由于第一层全连接层直接作用于像素输入，因此这些权重可以理解为不同隐藏神经元偏好的颜色分布和粗粒度空间模式。通过观察可视化结果，可以注意到：

- 某些权重图对蓝色、青色区域更敏感，这类模式可能与 `River`、`SeaLake` 等水体相关类别有关。
- 某些权重图呈现较明显的绿色或绿色纹理分布，可能与 `Forest`、`Pasture`、`HerbaceousVegetation` 等植被类场景有关。
- 一些权重图中存在块状明暗分布或较规则的区域结构，可能对应 `Residential`、`Industrial` 等人工地物较明显的场景。

不过，由于 MLP 缺乏卷积层对局部空间结构的建模能力，这些权重更多体现为**全局颜色偏好和大尺度纹理模式**，而不是 CNN 中常见的局部边缘、纹理核或角点响应。

---

### 5.6 错例分析

最优模型的部分错例以及主要混淆类别统计如下所示。

<div align="center">
  <img src="outputs/trial_07/error_cases.png" alt="Error Cases">
  <p><em>图 10  测试集中的部分错分样本</em></p>
</div>

<div align="center">
  <img src="outputs/trial_07/top_confusion_pairs.png" alt="Top Confusion Pairs">
  <p><em>图 11  最常见错误类别对统计图</em></p>
</div>

测试集中共出现 `1250` 个错误分类样本。导出的部分错例包括：

- `Residential -> HerbaceousVegetation`
- `SeaLake -> Forest`
- `Pasture -> Forest`
- `Highway -> River`
- `AnnualCrop -> HerbaceousVegetation`
- `Industrial -> Residential`
- `PermanentCrop -> AnnualCrop`
- `River -> Highway`

从具体样本层面分析，这些错分通常来自以下原因：

1. **局部形态相似**  
   `Highway` 和 `River` 在局部区域内都可能表现为细长曲线或带状结构，如果背景信息较弱，模型容易仅凭形状做出错误判断。

2. **纹理模式接近**  
   `AnnualCrop`、`PermanentCrop`、`HerbaceousVegetation` 和 `Pasture` 都具有大面积绿色纹理，单张图像中若缺少明显边界信息，容易被错分到相邻植被类别。

3. **人工地物混合出现**  
   `Industrial` 与 `Residential` 在一些样本中都包含规则建筑块、道路网络和灰白色区域，若场景中同时出现建筑与植被，模型更容易误判。

4. **场景上下文复杂**  
   `SeaLake -> Forest` 这一类错误说明，在某些样本中水体并非占据主导区域，岸边植被、阴影或周边地形会干扰模型判断。

---

## 六、实验结论

本实验完成了一个基于 `NumPy` 手写实现的三层 MLP 遥感图像分类系统，满足了作业对于手动前向传播、反向传播、损失函数、优化器、训练流程和测试评估的要求。

综合实验结果，可以得到以下结论：

1. 在 EuroSAT 数据集上，手写 MLP 可以达到约 `69%` 的测试准确率，说明即使不依赖卷积结构，仅凭标准化、合适的超参数和较大的隐藏层宽度，也能学习到一定的遥感图像判别信息。
2. 隐藏层规模与学习率对模型性能影响显著，较大的隐藏层结构 `(1024, 512)` 明显优于较小结构 `(256, 128)`。
3. 模型最容易在视觉结构或纹理统计相近的类别之间产生混淆，这与遥感场景的客观相似性相一致。
4. 第一层权重可视化表明，MLP 确实能够学习到一定的颜色与粗粒度空间模式，但其对局部结构的建模能力仍然弱于卷积网络。

总体来看，本实验较完整地验证了手写 MLP 在遥感图像分类任务中的实现流程与性能表现。若后续希望进一步提升精度，可以考虑：

- 使用更深的网络结构
- 引入卷积层替代纯全连接结构
- 加入数据增强
- 采用更系统的超参数搜索策略

---

## 七、提交材料补充说明

根据 PDF 要求，除实验报告和代码外，还需要补充以下提交材料：

1. 将代码上传到 Public GitHub Repo，并在 README 中写明环境依赖以及训练、测试方法。
2. 将最优模型权重上传到 Google Drive 等外部存储，并在报告中给出下载链接。

当前本地工程中，以下内容已经完成：

- 代码实现
- README 使用说明
- 训练脚本、测试脚本与超参数搜索脚本
- 超参数搜索结果汇总
- 测试集混淆矩阵
- 第一层权重可视化
- 错例分析结果

当前尚未实际完成的内容为：

- GitHub 仓库公开上传
- 模型权重外链上传
- 将本报告导出为 PDF

这三项需要在最终提交前补齐。
